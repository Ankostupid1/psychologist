[build]
  command = "npm run build"
  functions = "netlify-functions"
  publish = "#"  # or "build" depending on your framework

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
